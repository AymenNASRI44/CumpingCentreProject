{% extends 'base.html.twig' %}

{% block title %}Reservation index{% endblock %}

{% block body %}
    <h1>Reservation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date_d</th>
                <th>Date_f</th>
                <th>NombrePersonnes</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation in reservations %}
            <tr>
                <td>{{ reservation.id }}</td>
                <td>{{ reservation.dateD ? reservation.dateD|date('Y-m-d') : '' }}</td>
                <td>{{ reservation.dateF ? reservation.dateF|date('Y-m-d') : '' }}</td>
                <td>{{ reservation.nombrePersonnes }}</td>
                <td>
                    <a href="{{ path('app_crud_reservation_show', {'id': reservation.id}) }}">show</a>
                    <a href="{{ path('app_crud_reservation_edit', {'id': reservation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_crud_reservation_new') }}">Create new</a>
{% endblock %}
